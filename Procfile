# This configuration file is used by Heroku

# TODO: Modify this Procfile to fit your needs

# Web server that can handle Heroku traffic

# Heroku will run this command to start the app

web: gunicorn app:app
